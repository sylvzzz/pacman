# Packaging

Owner: Person B.

- `../pac-man.spec` — PyInstaller build specification (`make package` builds `dist/pac-man/`).
- `INSTRUCTIONS.txt` — shipped inside the build: controls, options, configuration.
- itch.io: upload `dist/pac-man/` as a free, unlisted build. Record the URL here.
